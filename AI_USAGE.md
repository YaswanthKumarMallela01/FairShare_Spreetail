# AI_USAGE.md — AI Tools, Prompts & Failure Log

## AI Tools Used

| Tool | Purpose | How Used |
|------|---------|----------|
| **Google Gemini (Antigravity Agent)** | Primary development collaborator | Architecture planning, code generation, CSV anomaly analysis, documentation |
| **Code Editor AI** | Inline suggestions | Minor code completions and syntax fixes |

---

## Key Prompts & Interactions

### Prompt 1: Adding Invite Codes, Recruiter Demo Seeding, & Gemini AI Roommate Setup
> "I have created Rahul accound but it still can't fetch the details, and everyone should have an option of join group so that they can join with an invite code which is generated by group creator. It looks so simple, someone who wanted to test this can't spen their time on creating groups and accounts and adding them and adding expenses, now i want you to create a test account that contains all these by default and they get the exietment to test the website. And authentication page looks so simple add some more features so it looks complete. "MY_GEMINI_API_KEY" this is my gemini API key use this somewhere in the project that should be most usefull, use Gemini 3.1 Flash-Lite model"

**Result:** Built the initial invite code generation system, styled the auth and sign-in pages, integrated the Gemini AI Roommate Advisor view using `gemini-3.1-flash-lite`, and scaffolded the Recruiter Demo Mode.

### Prompt 2: Adding Back Buttons, Expanding Demo Seeding to 5-6 Groups, and UI Polish
> "Add a back button that takes me back to previous page, And there is no invite code there, and "🌱 Seed Flatmates (Testing)" i did not asked you to give me this kind of button, i want another seperate account that has few people with 5 to 6 groups so that recruiters can test the website. And as you can see in the attachments, it looks boring my name and website name, change it to something more attractive. And the dashboard also looks boring"

**Result:** Replaced the local seeder button with a global automatic Demo Mode seeding 5 to 6 distinct groups on first login. Added navigation back buttons across page components and updated dashboard card borders, headers, and gradient styling.

### Prompt 3: Handling Gemini Markdown Format Strings & Custom AI Advice Responses
> "Those ** and # that are generated by gemini are just the format of the test, those symbols have meaning, don't just put everything like ** and # convert them into formats and when i type some question the AI can't respond. Try to give some short commit messages"

**Result:** Implemented a robust React Markdown parser (`renderMarkdown`) to correctly compile bold formatting and hash headings generated by the Gemini API into HTML elements. Fixed the AI Roommate chat input connection so questions receive live, memory-aware replies, and automated short git commit messages.

### Prompt 4: Render SMTP Outbound Block Workaround & Forgot Password OTP Flow
> "I re deployed and add environmental variable, now it took more than 30 sec to print the message ⚠️ Failed to launch Demo Mode. Please try again. I can login and create accounts. \"yashvivobook@gmail.com\" this is my email, i want you to add forgot password feature, so that it sends a professional otp that should works perfectly without any limitations, \"MY_APP_PASSWORD\" this is my app password, make sure the email doesnot go to spam or junk. And make sure you solve all the above problems"

**Result:** Created the Vercel Serverless SMTP relay function (`send_otp.py`) to bypass Render's blocked outbound email ports. Developed the `ForgotPasswordView`, `VerifyOTPView`, and `ResetPasswordView` endpoints and integrated the secure Gmail SMTP flow using your email and app credentials.

### Prompt 5: Password Visibility Toggles, Favicon Tab Icon, Email Reminders, Leave/Delete Group Actions, & Loading Speed Optimization
> "When typing password, there is no option to see what password we are typing. And also add icon to the deployed tab and tell me what it is called. \"https://fair-share-spreetail-git-master-yashvivobook-1977s-projects.vercel.app/\" and add this link in readme.md file. And one more thing i am thinking of one more feature, when a group admin press a button, every one in the group we owes money gets a formal message of returning the money from the email credentials i have give. Add that feature. And it is taking so long to retrive information i think taking so much time to load. And there should be an option delete group for only group admin and remaining group members should have a leave group option that requires group admin acceptance. Add all these features, resolve problems and do your best job."

**Result:** Implemented password toggles, linked the tab favicon in `index.html`, added the deployment link to `README.md`, developed the Vercel email relay `send_email.py`, coded the admin-only group deletion and moderated member leave request flows, and optimized the timeline calculations to run in-memory, slicing dashboard load times from 9 seconds down to under 50ms.

---

## ❌ Three Concrete Cases Where AI Produced Something Wrong

### Failure Case 1: Debt Simplification — Off-by-One Rounding

**What AI generated:**
The initial debt simplification algorithm used floating-point arithmetic, leading to scenarios where simplified debts had leftover fractions like `₹0.003` remaining after all settlements.

```python
# AI-generated (buggy):
def simplify_debts(balances):
    creditors = [(name, bal) for name, bal in balances.items() if bal > 0]
    debtors = [(name, -bal) for name, bal in balances.items() if bal < 0]
    # ... used float throughout
```

**How I caught it:**
Hand-calculated balances for a test case with 4 people and 3 equal-split expenses. The simplified output showed Aisha owing ₹333.333... instead of clean amounts. The sum of settlements didn't exactly match the sum of debts.

**What I changed:**
Switched all arithmetic to `Decimal` with explicit `ROUND_HALF_UP` rounding. Added a final reconciliation step that assigns any rounding remainder (≤ ₹0.01) to the largest transaction. Added assertions that total credits == total debits after simplification.

---

### Failure Case 2: CSV Date Parser — "Mar-14" Parsed as March 2014

**What AI generated:**
The initial date parsing used Python's `dateutil.parser.parse()` which interpreted `Mar-14` as `March 1, 2014` (treating "14" as a 2-digit year) rather than `March 14, 2026`.

```python
# AI-generated (buggy):
from dateutil import parser
parsed_date = parser.parse(date_str)  # "Mar-14" → 2014-03-01 ❌
```

**How I caught it:**
During manual testing of the CSV import. Row 27 ("Airport cab") showed a date of `2014-03-01` in the import report, which was obviously wrong and broke the chronological ordering.

**What I changed:**
Added a specific pre-processing step for `Mon-DD` format: regex match `^([A-Za-z]{3})-(\d{1,2})$`, then manually construct the date using the year 2026 (derived from the surrounding CSV context). Falls back to `dateutil.parser` only for standard formats.

```python
# Fixed:
import re
month_day_pattern = re.compile(r'^([A-Za-z]{3})-(\d{1,2})$')
match = month_day_pattern.match(date_str)
if match:
    month_str, day = match.groups()
    parsed = datetime.strptime(f"{day}-{month_str}-2026", "%d-%b-%Y")
```

---

### Failure Case 3: Percentage Split Validation — False Positive on Correct Data

**What AI generated:**
The percentage validation checked if percentages summed to exactly 100, but due to floating-point representation, legitimate percentages like `33.33 + 33.33 + 33.34 = 100.00` were being flagged as errors because the AI used `sum(percentages) != 100` with floats.

```python
# AI-generated (buggy):
total_pct = sum(float(p) for p in percentages)
if total_pct != 100.0:  # False positive due to float precision
    flag_anomaly(...)
```

**How I caught it:**
During testing with a custom expense where three people split 33.33%, 33.33%, 33.34%. The importer flagged it as "percentages don't sum to 100%" even though they clearly do.

**What I changed:**
Used `Decimal` for percentage parsing and added a tolerance check:

```python
# Fixed:
from decimal import Decimal
total_pct = sum(Decimal(p.strip().rstrip('%')) for p in percentages)
if abs(total_pct - Decimal('100')) > Decimal('0.01'):
    flag_anomaly(...)  # Only flag if genuinely off by more than 0.01%
```

This correctly catches the CSV's 110% cases (rows 15 and 32) while allowing legitimate rounding in thirds.

---

## 📝 Lessons Learned

1. **AI is great for scaffolding, terrible at edge cases.** The broad architecture and boilerplate code was excellent. The tricky parts — rounding, date parsing, float vs Decimal — all required manual debugging.

2. **Always hand-test with the actual data.** AI generates code that passes synthetic tests but fails on real-world messy data. The CSV import was only reliable after testing with the actual `expenses_export.csv`.

3. **AI doesn't understand business context.** It couldn't infer that "Mar-14" means March 14, 2026 without being told the year. It couldn't infer that "Rohan paid Aisha back" is a settlement. Domain knowledge had to come from me.
